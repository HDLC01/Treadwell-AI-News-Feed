# ─── Treadwell AI News Feed — backend requirements ───────────────────────
# The API runs in DEMO_MODE with ONLY the core deps below installed.
# Everything under "Pipeline / ingestion (optional)" is imported LAZILY inside
# the functions that use them, so a bare `pip install fastapi uvicorn pydantic
# pydantic-settings python-multipart` is enough to start the app and serve fixtures.

# ─── Core API (required to start the app) ────────────────────────────────
fastapi==0.137.1
# starlette is FastAPI's ASGI core — pinned in lockstep with the fastapi pin to
# clear 8 CVEs in 0.41.x (PYSEC-2026-161, CVE-2025-54121/62727, CVE-2026-48817/
# 48818/54282/54283). Matches the proposal-tool fix.
starlette==1.3.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
# 0.0.31 clears CVE-2026-53538/53539/53540 (DoS in multipart parsing).
python-multipart==0.0.31
# tzdata: bundle the IANA tz database so ZoneInfo("America/Chicago") resolves
# even on a minimal base image — keeps every human-facing date pinned to Central.
tzdata>=2024.1
# Auth: verify Supabase/Google JWTs (RS256 via JWKS needs the crypto extra).
# 2.13.0 clears PYSEC-2025-183 + PYSEC-2026-175/176/177/178/179.
PyJWT[crypto]==2.13.0

# ─── Database (lazy: only imported when Supabase is configured) ───────────
supabase==2.11.0

# ─── Pipeline / ingestion (lazy: only imported during a pipeline run) ─────
httpx==0.28.1
feedparser==6.0.11
beautifulsoup4==4.12.3
# 6.1.0 clears PYSEC-2026-87 (lxml is lazy — only used during pipeline ingestion).
lxml==6.1.0
apscheduler==3.11.0
python-dateutil==2.9.0.post0

# ─── Email (lazy: Resend is called over plain HTTP via httpx) ─────────────
# No separate SDK needed — mailer.py POSTs to the Resend REST API with httpx.
